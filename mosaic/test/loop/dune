; MIGRATION NOTE (windtrap 0.2): see mosaic/test/expect/dune — this was
; an (executables) stanza whose let%expect_test bodies never ran.

(library
 (name mosaic_loop_expect)
 (inline_tests)
 (libraries
  windtrap
  mosaic
  matrix
  matrix.test
  matrix.input
  matrix.vte
  threads.posix)
 (preprocess
  (pps ppx_windtrap)))
