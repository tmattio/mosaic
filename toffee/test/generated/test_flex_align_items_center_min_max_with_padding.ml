(* Generated test for align_items_center_min_max_with_padding in flex layout *)
(* Do not edit this file directly. It is generated by gentest.js *)

open Toffee

let test_flex_align_items_center_min_max_with_padding_border_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node1 =
    new_leaf tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 62.0;
             height = Style.Dimension.length 62.0;
           }
         ())
    |> Result.get_ok
  in
  let node0 =
    new_with_children tree
      (Style.make ~flex_direction:Style.Flex_direction.Row ~align_items:Center
         ~min_size:
           {
             width = Style.Dimension.length 320.0;
             height = Style.Dimension.length 72.0;
           }
         ~max_size:
           {
             width = Style.Dimension.length 320.0;
             height = Style.Dimension.length 504.0;
           }
         ~padding:
           {
             left = Style.Length_percentage.length 0.0;
             right = Style.Length_percentage.length 0.0;
             top = Style.Length_percentage.length 8.0;
             bottom = Style.Length_percentage.length 8.0;
           }
         ())
      [| node1 |]
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 62.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 62.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 8.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 320.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 78.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

let test_flex_align_items_center_min_max_with_padding_content_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node1 =
    new_leaf tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 62.0;
             height = Style.Dimension.length 62.0;
           }
         ~box_sizing:Style.Box_sizing.Content_box ())
    |> Result.get_ok
  in
  let node0 =
    new_with_children tree
      (Style.make ~flex_direction:Style.Flex_direction.Row ~align_items:Center
         ~min_size:
           {
             width = Style.Dimension.length 320.0;
             height = Style.Dimension.length 72.0;
           }
         ~max_size:
           {
             width = Style.Dimension.length 320.0;
             height = Style.Dimension.length 504.0;
           }
         ~padding:
           {
             left = Style.Length_percentage.length 0.0;
             right = Style.Length_percentage.length 0.0;
             top = Style.Length_percentage.length 8.0;
             bottom = Style.Length_percentage.length 8.0;
           }
         ~box_sizing:Style.Box_sizing.Content_box ())
      [| node1 |]
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 62.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 62.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 13.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 320.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 88.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

(* Export tests for aggregation *)
let tests =
  let open Alcotest in
  [
    test_case "align_items_center_min_max_with_padding (border-box)" `Quick
      test_flex_align_items_center_min_max_with_padding_border_box;
    test_case "align_items_center_min_max_with_padding (content-box)" `Quick
      test_flex_align_items_center_min_max_with_padding_content_box;
  ]
