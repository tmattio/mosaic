(* Generated test for grid_padding_border_overrides_size in grid layout *)
(* Do not edit this file directly. It is generated by gentest.js *)

open Toffee

let test_grid_padding_border_overrides_size_border_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node =
    new_leaf tree (Style.make ~display:Style.Display.Grid ()) |> Result.get_ok
  in
  let node0 =
    new_leaf tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 12.0;
             height = Style.Dimension.length 12.0;
           }
         ~padding:
           {
             left = Style.Length_percentage.length 8.0;
             right = Style.Length_percentage.length 4.0;
             top = Style.Length_percentage.length 2.0;
             bottom = Style.Length_percentage.length 6.0;
           }
         ~border:
           {
             left = Style.Length_percentage.length 7.0;
             right = Style.Length_percentage.length 3.0;
             top = Style.Length_percentage.length 1.0;
             bottom = Style.Length_percentage.length 5.0;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node node0 |> Result.get_ok in

  (* Compute layout *)
  let _ =
    compute_layout tree node
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node |> Result.get_ok in
  assert_eq ~msg:"width of node" 22.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node" 14.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 22.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 14.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

let test_grid_padding_border_overrides_size_content_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node =
    new_leaf tree (Style.make ~display:Style.Display.Grid ()) |> Result.get_ok
  in
  let node0 =
    new_leaf tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 12.0;
             height = Style.Dimension.length 12.0;
           }
         ~padding:
           {
             left = Style.Length_percentage.length 8.0;
             right = Style.Length_percentage.length 4.0;
             top = Style.Length_percentage.length 2.0;
             bottom = Style.Length_percentage.length 6.0;
           }
         ~border:
           {
             left = Style.Length_percentage.length 7.0;
             right = Style.Length_percentage.length 3.0;
             top = Style.Length_percentage.length 1.0;
             bottom = Style.Length_percentage.length 5.0;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node node0 |> Result.get_ok in

  (* Compute layout *)
  let _ =
    compute_layout tree node
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node |> Result.get_ok in
  assert_eq ~msg:"width of node" 34.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node" 26.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 34.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 26.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

(* Export tests for aggregation *)
let tests =
  let open Alcotest in
  [
    test_case "padding_border_overrides_size (border-box)" `Quick
      test_grid_padding_border_overrides_size_border_box;
    test_case "padding_border_overrides_size (content-box)" `Quick
      test_grid_padding_border_overrides_size_content_box;
  ]
