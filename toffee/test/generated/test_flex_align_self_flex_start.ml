(* Generated test for align_self_flex_start in flex layout *)
(* Do not edit this file directly. It is generated by gentest.js *)

open Toffee

let test_flex_align_self_flex_start_border_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node1 =
    new_leaf tree
      (Style.make ~align_self:Flex_start
         ~size:
           {
             width = Style.Dimension.length 10.0;
             height = Style.Dimension.length 10.0;
           }
         ())
    |> Result.get_ok
  in
  let node0 =
    new_with_children tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 100.0;
             height = Style.Dimension.length 100.0;
           }
         ())
      [| node1 |]
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 10.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 10.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 100.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

let test_flex_align_self_flex_start_content_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node1 =
    new_leaf tree
      (Style.make ~align_self:Flex_start
         ~size:
           {
             width = Style.Dimension.length 10.0;
             height = Style.Dimension.length 10.0;
           }
         ~box_sizing:Style.Box_sizing.Content_box ())
    |> Result.get_ok
  in
  let node0 =
    new_with_children tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 100.0;
             height = Style.Dimension.length 100.0;
           }
         ~box_sizing:Style.Box_sizing.Content_box ())
      [| node1 |]
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 10.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 10.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 100.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

(* Export tests for aggregation *)
let tests =
  let open Alcotest in
  [
    test_case "align_self_flex_start (border-box)" `Quick
      test_flex_align_self_flex_start_border_box;
    test_case "align_self_flex_start (content-box)" `Quick
      test_flex_align_self_flex_start_content_box;
  ]
