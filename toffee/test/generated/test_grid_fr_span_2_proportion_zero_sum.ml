(* Generated test for grid_fr_span_2_proportion_zero_sum in grid layout *)
(* Do not edit this file directly. It is generated by gentest.js *)

open Toffee

let test_grid_fr_span_2_proportion_zero_sum_border_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = Gentest_helpers.new_test_tree () in

  (* Create nodes *)
  let node1 =
    new_leaf tree
      (Style.make
         ~grid_column:
           {
             start = Style.Grid.Placement.span 2;
             end_ = Style.Grid.Placement.auto;
           }
         ~size:
           {
             width = Style.Dimension.length 60.0;
             height = Style.Dimension.auto;
           }
         ~box_sizing:Style.Box_sizing.Border_box ())
    |> Result.get_ok
  in
  let node2 =
    new_leaf tree (Style.make ~box_sizing:Style.Box_sizing.Border_box ())
    |> Result.get_ok
  in
  let node3 =
    new_leaf tree (Style.make ~box_sizing:Style.Box_sizing.Border_box ())
    |> Result.get_ok
  in
  let node0 =
    new_with_children tree
      (Style.make ~display:Style.Display.Grid
         ~grid_template_columns:
           [
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.fr 0.0);
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.fr 0.0);
           ]
         ~grid_template_rows:
           [
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.length 40.0);
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.length 40.0);
           ]
         ~box_sizing:Style.Box_sizing.Border_box ())
      [| node1; node2; node3 |]
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout_with_measure tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
      Gentest_helpers.test_measure_function
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 60.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 40.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node2 |> Result.get_ok in
  assert_eq ~msg:"width of node2" 30.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node2" 40.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node2" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node2" 40.0 (Layout.location layout_result).y;
  let layout_result = layout tree node3 |> Result.get_ok in
  assert_eq ~msg:"width of node3" 30.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node3" 40.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node3" 30.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node3" 40.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 60.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 80.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

let test_grid_fr_span_2_proportion_zero_sum_content_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = Gentest_helpers.new_test_tree () in

  (* Create nodes *)
  let node1 =
    new_leaf tree
      (Style.make
         ~grid_column:
           {
             start = Style.Grid.Placement.span 2;
             end_ = Style.Grid.Placement.auto;
           }
         ~size:
           {
             width = Style.Dimension.length 60.0;
             height = Style.Dimension.auto;
           }
         ~box_sizing:Style.Box_sizing.Content_box ())
    |> Result.get_ok
  in
  let node2 =
    new_leaf tree (Style.make ~box_sizing:Style.Box_sizing.Content_box ())
    |> Result.get_ok
  in
  let node3 =
    new_leaf tree (Style.make ~box_sizing:Style.Box_sizing.Content_box ())
    |> Result.get_ok
  in
  let node0 =
    new_with_children tree
      (Style.make ~display:Style.Display.Grid
         ~grid_template_columns:
           [
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.fr 0.0);
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.fr 0.0);
           ]
         ~grid_template_rows:
           [
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.length 40.0);
             Style.Grid.Template_component.single
               (Style.Grid.Track_sizing_function.length 40.0);
           ]
         ~box_sizing:Style.Box_sizing.Content_box ())
      [| node1; node2; node3 |]
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout_with_measure tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
      Gentest_helpers.test_measure_function
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 60.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 40.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node2 |> Result.get_ok in
  assert_eq ~msg:"width of node2" 30.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node2" 40.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node2" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node2" 40.0 (Layout.location layout_result).y;
  let layout_result = layout tree node3 |> Result.get_ok in
  assert_eq ~msg:"width of node3" 30.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node3" 40.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node3" 30.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node3" 40.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 60.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 80.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

(* Export tests for aggregation *)
let tests =
  let open Alcotest in
  [
    test_case "fr_span_2_proportion_zero_sum (border-box)" `Quick
      test_grid_fr_span_2_proportion_zero_sum_border_box;
    test_case "fr_span_2_proportion_zero_sum (content-box)" `Quick
      test_grid_fr_span_2_proportion_zero_sum_content_box;
  ]
