(* Generated test for intrinsic_sizing_cross_size_column in flex layout *)
(* Do not edit this file directly. It is generated by gentest.js *)

open Toffee

let test_flex_intrinsic_sizing_cross_size_column_border_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = Gentest_helpers.new_test_tree () in

  (* Create nodes *)
  let node0 =
    new_leaf tree
      (Style.make ~flex_direction:Style.Flex_direction.Column
         ~box_sizing:Style.Box_sizing.Border_box ())
    |> Result.get_ok
  in
  let _ =
    set_node_context tree node0
      (Some
         (Gentest_helpers.ahem_text "HH​HH"
            Gentest_helpers.Writing_mode.Horizontal))
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout_with_measure tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
      Gentest_helpers.test_measure_function
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 40.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 10.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

let test_flex_intrinsic_sizing_cross_size_column_content_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = Gentest_helpers.new_test_tree () in

  (* Create nodes *)
  let node0 =
    new_leaf tree
      (Style.make ~flex_direction:Style.Flex_direction.Column
         ~box_sizing:Style.Box_sizing.Content_box ())
    |> Result.get_ok
  in
  let _ =
    set_node_context tree node0
      (Some
         (Gentest_helpers.ahem_text "HH​HH"
            Gentest_helpers.Writing_mode.Horizontal))
    |> Result.get_ok
  in

  (* Compute layout *)
  let _ =
    compute_layout_with_measure tree node0
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
      Gentest_helpers.test_measure_function
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node0;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 40.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 10.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  ()

(* Export tests for aggregation *)
let tests =
  let open Alcotest in
  [
    test_case "intrinsic_sizing_cross_size_column (border-box)" `Quick
      test_flex_intrinsic_sizing_cross_size_column_border_box;
    test_case "intrinsic_sizing_cross_size_column (content-box)" `Quick
      test_flex_intrinsic_sizing_cross_size_column_content_box;
  ]
