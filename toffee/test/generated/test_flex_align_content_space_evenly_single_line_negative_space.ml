(* Generated test for align_content_space_evenly_single_line_negative_space in flex layout *)
(* Do not edit this file directly. It is generated by gentest.js *)

open Toffee

let test_flex_align_content_space_evenly_single_line_negative_space_border_box
    () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node =
    new_leaf tree
      (Style.make ~display:Style.Display.Flex
         ~flex_direction:Style.Flex_direction.Column
         ~size:
           {
             width = Style.Dimension.length 320.0;
             height = Style.Dimension.length 320.0;
           }
         ~border:
           {
             left = Style.Length_percentage.length 60.0;
             right = Style.Length_percentage.length 60.0;
             top = Style.Length_percentage.length 60.0;
             bottom = Style.Length_percentage.length 60.0;
           }
         ())
    |> Result.get_ok
  in
  let node0 =
    new_leaf tree
      (Style.make ~display:Style.Display.Flex
         ~flex_direction:Style.Flex_direction.Row ~justify_content:Center
         ~align_content:Space_evenly
         ~size:
           {
             width = Style.Dimension.auto;
             height = Style.Dimension.length 10.0;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node node0 |> Result.get_ok in
  let node1 =
    new_leaf tree
      (Style.make ~flex_shrink:0.0
         ~size:
           {
             width = Style.Dimension.percent 0.8;
             height = Style.Dimension.length 60.0;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node0 node1 |> Result.get_ok in

  (* Compute layout *)
  let _ =
    compute_layout tree node
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node |> Result.get_ok in
  assert_eq ~msg:"width of node" 320.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node" 320.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 200.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 10.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 60.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 60.0 (Layout.location layout_result).y;
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 160.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 60.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 20.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  ()

let test_flex_align_content_space_evenly_single_line_negative_space_content_box
    () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node =
    new_leaf tree
      (Style.make ~display:Style.Display.Flex
         ~flex_direction:Style.Flex_direction.Column
         ~size:
           {
             width = Style.Dimension.length 320.0;
             height = Style.Dimension.length 320.0;
           }
         ~border:
           {
             left = Style.Length_percentage.length 60.0;
             right = Style.Length_percentage.length 60.0;
             top = Style.Length_percentage.length 60.0;
             bottom = Style.Length_percentage.length 60.0;
           }
         ())
    |> Result.get_ok
  in
  let node0 =
    new_leaf tree
      (Style.make ~display:Style.Display.Flex
         ~flex_direction:Style.Flex_direction.Row ~justify_content:Center
         ~align_content:Space_evenly
         ~size:
           {
             width = Style.Dimension.auto;
             height = Style.Dimension.length 10.0;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node node0 |> Result.get_ok in
  let node1 =
    new_leaf tree
      (Style.make ~flex_shrink:0.0
         ~size:
           {
             width = Style.Dimension.percent 0.8;
             height = Style.Dimension.length 60.0;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node0 node1 |> Result.get_ok in

  (* Compute layout *)
  let _ =
    compute_layout tree node
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node |> Result.get_ok in
  assert_eq ~msg:"width of node" 440.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node" 440.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 320.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 10.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 60.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 60.0 (Layout.location layout_result).y;
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 256.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 60.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 32.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  ()

(* Export tests for aggregation *)
let tests =
  let open Alcotest in
  [
    test_case
      "align_content_space_evenly_single_line_negative_space (border-box)"
      `Quick
      test_flex_align_content_space_evenly_single_line_negative_space_border_box;
    test_case
      "align_content_space_evenly_single_line_negative_space (content-box)"
      `Quick
      test_flex_align_content_space_evenly_single_line_negative_space_content_box;
  ]
