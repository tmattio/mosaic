(* Generated test for flex_grow_within_constrained_max_row in flex layout *)
(* Do not edit this file directly. It is generated by gentest.js *)

open Toffee

let test_flex_grow_within_constrained_max_row_border_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node =
    new_leaf tree
      (Style.make ~flex_direction:Style.Flex_direction.Column
         ~size:
           {
             width = Style.Dimension.length 200.0;
             height = Style.Dimension.auto;
           }
         ())
    |> Result.get_ok
  in
  let node0 =
    new_leaf tree
      (Style.make ~flex_direction:Style.Flex_direction.Row
         ~size:
           {
             width = Style.Dimension.auto;
             height = Style.Dimension.length 100.0;
           }
         ~max_size:
           {
             width = Style.Dimension.length 100.0;
             height = Style.Dimension.auto;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node node0 |> Result.get_ok in
  let node1 =
    new_leaf tree
      (Style.make ~flex_shrink:1.0
         ~flex_basis:(Style.Dimension.length 100.0)
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node0 node1 |> Result.get_ok in
  let node2 =
    new_leaf tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 50.0;
             height = Style.Dimension.auto;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node0 node2 |> Result.get_ok in

  (* Compute layout *)
  let _ =
    compute_layout tree node
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node |> Result.get_ok in
  assert_eq ~msg:"width of node" 200.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 100.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 67.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node2 |> Result.get_ok in
  assert_eq ~msg:"width of node2" 33.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node2" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node2" 67.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node2" 0.0 (Layout.location layout_result).y;
  ()

let test_flex_grow_within_constrained_max_row_content_box () =
  (* Setup test helpers *)
  let assert_eq ~msg expected actual =
    let open Alcotest in
    check (float 0.001) msg expected actual
  in

  let tree = new_tree () in

  (* Create nodes *)
  let node =
    new_leaf tree
      (Style.make ~flex_direction:Style.Flex_direction.Column
         ~size:
           {
             width = Style.Dimension.length 200.0;
             height = Style.Dimension.auto;
           }
         ())
    |> Result.get_ok
  in
  let node0 =
    new_leaf tree
      (Style.make ~flex_direction:Style.Flex_direction.Row
         ~size:
           {
             width = Style.Dimension.auto;
             height = Style.Dimension.length 100.0;
           }
         ~max_size:
           {
             width = Style.Dimension.length 100.0;
             height = Style.Dimension.auto;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node node0 |> Result.get_ok in
  let node1 =
    new_leaf tree
      (Style.make ~flex_shrink:1.0
         ~flex_basis:(Style.Dimension.length 100.0)
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node0 node1 |> Result.get_ok in
  let node2 =
    new_leaf tree
      (Style.make
         ~size:
           {
             width = Style.Dimension.length 50.0;
             height = Style.Dimension.auto;
           }
         ())
    |> Result.get_ok
  in
  let _ = add_child tree node0 node2 |> Result.get_ok in

  (* Compute layout *)
  let _ =
    compute_layout tree node
      {
        width = Available_space.Max_content;
        height = Available_space.Max_content;
      }
    |> Result.get_ok
  in

  (* Print tree for debugging *)
  Printf.printf "\nComputed tree:\n";
  print_tree tree node;
  Printf.printf "\n";

  (* Verify layout *)
  let layout_result = layout tree node |> Result.get_ok in
  assert_eq ~msg:"width of node" 200.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node0 |> Result.get_ok in
  assert_eq ~msg:"width of node0" 100.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node0" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node0" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node0" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node1 |> Result.get_ok in
  assert_eq ~msg:"width of node1" 67.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node1" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node1" 0.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node1" 0.0 (Layout.location layout_result).y;
  let layout_result = layout tree node2 |> Result.get_ok in
  assert_eq ~msg:"width of node2" 33.0 (Layout.size layout_result).width;
  assert_eq ~msg:"height of node2" 100.0 (Layout.size layout_result).height;
  assert_eq ~msg:"x of node2" 67.0 (Layout.location layout_result).x;
  assert_eq ~msg:"y of node2" 0.0 (Layout.location layout_result).y;
  ()

(* Export tests for aggregation *)
let tests =
  let open Alcotest in
  [
    test_case "grow_within_constrained_max_row (border-box)" `Quick
      test_flex_grow_within_constrained_max_row_border_box;
    test_case "grow_within_constrained_max_row (content-box)" `Quick
      test_flex_grow_within_constrained_max_row_content_box;
  ]
