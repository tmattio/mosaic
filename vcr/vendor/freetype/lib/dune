(rule
 (targets c_flags c_library_flags)
 (deps
  (:discover ../config/discover.exe))
 (action
  (run %{discover})))

(library
 (name freetype)
 (public_name vcr.freetype)
 (foreign_stubs
  (language c)
  (names freetype_stubs)
  (flags
   (:standard
    (:include c_flags))))
 (c_library_flags
  (:standard
   (:include c_library_flags))))
